Array.prototype.reduce = Array.prototype.reduce || function reduce(func,initValue){
    let arr = this
    let initIndex = initValue ? 1 : 0
    let initValue = initValueArr ? initValueArr[0] : arr[0]
    
}